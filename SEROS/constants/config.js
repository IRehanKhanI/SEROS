// ─── CONFIG ────────────────────────────────────────────────────────────────────
// Update this IP when the Django server changes network
export const BACKEND_IP = "10.1.12.187";
export const BACKEND_PORT = "8000";
export const BACKEND_URL = `http://${BACKEND_IP}:${BACKEND_PORT}`;

// Camera settings
export const CAPTURE_MS = 300;    
export const IMAGE_QUALITY = 0.45;   
export const REQUEST_TIMEOUT_MS = 3000; 
// ──────────────────────────────────────────────────────────────────────────────
